import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container:{
       flex: 1
    },
    img:{
      flex:1,
      resizeMode:'cover',
    }
    
  })

export default styles